# LuxOfflineTool
This tool generates the `offline.db` file downloaded by Lux when offline mode is enabled

It obtains the latest GTFS dataset from OpenTransportData.swiss.

This feature is only available since >=0.8.


